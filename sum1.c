#include<stdio.h>
#include<math.h>
void main()
{
float n,i=1,x,sum=0;
printf("enter number of term=");
scanf("%f",&n);
printf("enter value of x");
scanf("%f",&x);
while(i<=n)
{
 sum=sum+i/(x,i);
 i++;
 }
 printf("sum=%f",sum);
 }
